package com.vizuri.fantasy_football_brms;

import java.lang.Number;
import com.vizuri.rules.domain.FantasyTeam;
import com.vizuri.rules.domain.Roster;
import com.vizuri.rules.domain.Player;

rule "RosterRule"
	dialect "mvel"
	when
		FantasyTeam( roster : roster != null , roster.size <= 15 )
	then
end
