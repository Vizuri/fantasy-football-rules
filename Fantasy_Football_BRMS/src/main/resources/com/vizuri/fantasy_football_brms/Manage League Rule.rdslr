package com.vizuri.fantasy_football_brms;

import java.lang.Number;

rule "Manage League Rule"
	dialect "mvel"
	when
		There is a roster
		- When the roster has a fantasy team size greater than 15
		- When the roster has sizeValid that is true
	then
end
