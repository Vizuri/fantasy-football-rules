package com.vizuri.fantasy_football_brms;

import com.vizuri.rules.domain.League;

rule "League Violation Rule"
	dialect "mvel"
	when
		There is a league
		- The league has fantasyTeamsCount greater than 8
		- The league has valid that is true
	then
		Invalidate the league
end
