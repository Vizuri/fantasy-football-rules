package com.vizuri.fantasy_football_brms;

import com.vizuri.rules.domain.Roster;

rule "RosterRuleSize"
	dialect "mvel"
	when
		roster : Roster( size > 15 , sizeValid == true )
	then
		roster.setSizeValid( false );
		System.out.println("The Roster size is more than 15");
end
